import { StyleSheet } from 'react-native';
import theme from './theme.style';

export default StyleSheet.create({
    container: {
        borderRadius: 10,
        overflow: 'hidden',
        elevation: 2,
        backgroundColor: '#fff',
        marginHorizontal: 20
    },
    image: {
        height: 200,
        alignSelf: 'stretch',
    },
    title: {
        fontWeight: 'bold',
        fontSize: 18,
    },
    caption: {
        
    }

});
