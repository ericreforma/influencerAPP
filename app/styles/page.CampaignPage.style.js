import { StyleSheet } from 'react-native';
import theme from './theme.style';

export default StyleSheet.create({
    modalContainer: {
        backgroundColor: '#fff',
        padding: 25,
        borderRadius: 20,
    },
    modalText: {
        alignSelf: 'center',
        fontSize: 18,
        marginBottom: 25,
    },
    
});
